# Lightweight backend dependencies for local MOCK_LLM=true development.
# This intentionally skips torch/transformers/sentence-transformers so Docker
# does not download large CUDA wheels when only the mock responder is needed.

# --- web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.10

# --- db ---
sqlalchemy[asyncio]==2.0.35
psycopg[binary]==3.2.3
alembic==1.13.3

# --- cache / queue / lock ---
redis==5.1.1

# --- object store ---
minio==7.2.9

# --- vector store ---
qdrant-client>=1.18.0

# --- lightweight retrieval support ---
rank-bm25==0.2.2
numpy==1.26.4
huggingface-hub==0.25.2

# --- security ---
argon2-cffi==23.1.0
cryptography==43.0.1
PyJWT==2.9.0

# --- utils ---
python-dotenv==1.0.1
httpx==0.27.2
email-validator==2.2.0
